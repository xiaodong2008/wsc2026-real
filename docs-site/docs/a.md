# Module A

0.5 / 1 / 1.5 是按官方「分值对应用时」推出来的，不是试卷上印的分数。Q&A 如果问到真实分值，按真实的重排。

做题顺序按这个推断：A07、A25、C06、A12、C08、A23、C09、B08，然后看表。B19 不提前拍死做不做。做完 B08 时如果距 12:35 还够把它做完，就做；不够就跳过。B30、B40 放最后。

12:35 之后只巡检，不写新代码。不要为了凑满 14 个仓库去建空仓库。按计划少做一题，就是 12 或 13 个，这是正常的。

09:45 先花 2 分钟看 media：选择器名、stub 里已有的变量、SQL 列名、mp4 里的动作。这些赛前对不上。

每题一个仓库。名字用 `A07`、`B08`、`C06` 这种两位数字，不要加 `-E`。写完一题就提交，说明写成一句人话。

静态题把素材盖进 Vanilla 模板后，删掉模板的 `index.php`，否则首页仍是模板演示。

C08、C09 选 Express 模板，但 `server.js` 用 Node 自带的 `http`，不能 `require('express')`。

片段都在 [考场粘贴](/paste)。这里只记限制：

| 题 | 只能改 |
| --- | --- |
| A07 | `css/style.css` 标记以下 |
| A12 | `style.css` |
| A23 | `style.css` 标记以下 |
| A25 | 单个 `index.html` |
| B08 | `js/script.js` 标记以下 |
| B19 | `script.js` 标记以下 |
| C06 | `result/result.sql` |
| C08 / C09 | `server.js` 里对应的路由 |
