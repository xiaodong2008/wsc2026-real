How to run
1. npm install
2. npm run dev
3. Open the site and use the hash routes #/schedule #/map #/stamps #/photo

Docker: docker compose up --build
The competition platform serves this repository on your subdomain. Static JSON and images live in public/ and are loaded with relative paths. There is no backend.
